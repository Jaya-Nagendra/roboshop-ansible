- name: Install mysql client
  ansible.builtin.yum:
    name: mysql
    state: installed    

- name: load mysql schema
  ansible.builtin.shell: mysql -h mysql.ljnag.space -uroot -pRoboShop@1 < /app/db/{{component}}.sql     
